<%- include('partials/head')  %>


<%- include('partials/navbar')  %>

<img src="" alt="">
<h1><%= firstTitle %> by <%= poetName %> </h2>

    <% for (let i = 0; i < firstLines.length; i++){ %>
    <p class="line"><%= firstLines[i] %> </p>
    <% } %>